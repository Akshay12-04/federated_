# Domain (pure logic)

- `encodingLoader.js` — loads `data/encoding-manifest.json`
- `wizardToFeatures.js` — wizard → 29-feature vector
- `validation.js` — per-step validation
- `riskEngine.js` — mock `scoreRisk(features)` → `RiskResult`
